# 🧪 Тестирование PvP матчей через Swagger UI

**Статус:** ✅ API готов к интерактивному тестированию

---

## 🚀 Быстрый старт

### 1. Запустить PostgreSQL

```bash
cd /Users/andrewUG/VS_code/olympiad_platform
docker-compose up -d postgres
sleep 5
```

### 2. Запустить FastAPI сервер

```bash
cd backend
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 3. Открыть Swagger UI

Перейти в браузер:
```
http://localhost:8000/docs
```

Вы увидите интерактивную документацию API со всеми endpoints.

---

## 📋 Пошаговое тестирование сценария

### Сценарий: Создание матча и отправка ответов

#### Шаг 1: Создать матч

1. Открыть **POST /matches** → "Создать новый матч"
2. Нажать **"Try it out"**
3. В **Request body** введите:
   ```json
   {
     "player1_id": 1,
     "player2_id": 2
   }
   ```
4. Нажать **Execute**
5. **Response:** Получите `match_id: 1` (запомните его!)

**Пример ответа:**
```json
{
  "id": 1,
  "player1_id": 1,
  "player2_id": 2,
  "status": "waiting",
  "player1_score": 0,
  "player2_score": 0,
  "winner_id": null,
  "created_at": "2026-02-05T15:33:17.015391",
  "message": "✓ Матч создан, ожидание второго игрока (статус: waiting)"
}
```

---

#### Шаг 2: Добавить задачи в матч

1. Открыть **POST /matches/{match_id}/tasks** → "Добавить задачи в матч"
2. Нажать **"Try it out"**
3. В **Path parameter** укажите: `match_id = 1`
4. В **Request body** введите:
   ```json
   {
     "task_ids": [1, 2, 3]
   }
   ```
5. Нажать **Execute**

**Пример ответа:**
```json
{
  "match_id": 1,
  "message": "✓ Добавлено 3 задач в матч",
  "task_ids": [1, 2, 3]
}
```

---

#### Шаг 3: Отправить ответ игрока 1 на задачу 1

1. Открыть **POST /matches/{match_id}/answers** → "Отправить ответ на задачу"
2. Нажать **"Try it out"**
3. Заполнить параметры:
   - `match_id` = 1
   - `task_id` = 1
   - `user_id` = 1
4. В **Request body** введите:
   ```json
   {
     "answer": "правильный ответ"
   }
   ```
5. Нажать **Execute**

**Пример ответа (INSERT):**
```json
{
  "action": "created",
  "message": "✓ Ответ отправлен (INSERT паттерн)",
  "data": {
    "match_id": 1,
    "user_id": 1,
    "task_id": 1,
    "is_correct": true,
    "submitted_at": "2026-02-05T15:33:17.035575"
  }
}
```

---

#### Шаг 4: Повторная отправка ответа (UPSERT)

1. Открыть **POST /matches/{match_id}/answers** → "Отправить ответ на задачу"
2. Заполнить **ТЕ ЖЕ** параметры (match_id=1, task_id=1, user_id=1)
3. Но в **Request body** введите другой ответ:
   ```json
   {
     "answer": "новый ответ"
   }
   ```
4. Нажать **Execute**

**Пример ответа (UPDATE):**
```json
{
  "action": "updated",
  "message": "✓ Ответ обновлён (UPDATE паттерн)",
  "data": {
    "match_id": 1,
    "user_id": 1,
    "task_id": 1,
    "is_correct": false,
    "submitted_at": "2026-02-05T15:33:17.057012"
  }
}
```

**Заметьте:** `submitted_at` обновилась! Это доказывает, что произошёл UPDATE, а не INSERT.

---

#### Шаг 5: Получить информацию о матче

1. Открыть **GET /matches/{match_id}** → "Получить информацию о матче"
2. Нажать **"Try it out"**
3. В **Path parameter** укажите: `match_id = 1`
4. Нажать **Execute**

**Пример ответа:**
```json
{
  "match": {
    "id": 1,
    "player1": {
      "id": 1,
      "username": "alice",
      "rating": 1200,
      "score": 1
    },
    "player2": {
      "id": 2,
      "username": "bob",
      "rating": 1100,
      "score": 0
    },
    "status": "waiting",
    "created_at": "2026-02-05T15:33:17.015391",
    "finished_at": null
  },
  "tasks": [
    {
      "order": 1,
      "task_id": 1,
      "title": "Сумма двух чисел",
      "difficulty": 1
    },
    {
      "order": 2,
      "task_id": 2,
      "title": "Поиск максимума",
      "difficulty": 2
    },
    {
      "order": 3,
      "task_id": 3,
      "title": "Сортировка массива",
      "difficulty": 3
    }
  ],
  "answers": [
    {
      "user_id": 1,
      "task_id": 1,
      "is_correct": false,
      "submitted_at": "2026-02-05T15:33:17.057012"
    }
  ]
}
```

---

#### Шаг 6: Завершить матч и расчитать рейтинг

1. Открыть **POST /matches/{match_id}/finish** → "Завершить матч"
2. Нажать **"Try it out"**
3. В **Path parameter** укажите: `match_id = 1`
4. Нажать **Execute**

**Пример ответа:**
```json
{
  "match_id": 1,
  "status": "finished",
  "player1": {
    "id": 1,
    "score": 1,
    "rating_change": -25
  },
  "player2": {
    "id": 2,
    "score": 0,
    "rating_change": 25
  },
  "winner_id": 1,
  "finished_at": "2026-02-05T15:33:17.282272",
  "message": "✓ Матч завершён"
}
```

---

#### Шаг 7: Получить список активных матчей

1. Открыть **GET /matches** → "Список активных матчей"
2. Нажать **"Try it out"**
3. Нажать **Execute**

**Пример ответа:**
```json
{
  "total": 0,
  "matches": []
}
```

(Матч уже завершён, поэтому в активных не отображается)

---

## 🎯 Практические примеры

### Пример 1: Протестировать UPSERT паттерн

1. Отправить ответ на задачу (INSERT)
2. Отправить ещё раз **тот же** ответ с изменением (UPDATE)
3. Проверить в **GET /matches/{id}**, что в ответах только ОДИН ответ на эту задачу

### Пример 2: Протестировать CHECK constraint (player1 ≠ player2)

1. Открыть **POST /matches** → "Создать новый матч"
2. Нажать **"Try it out"**
3. Введите:
   ```json
   {
     "player1_id": 1,
     "player2_id": 1
   }
   ```
4. Нажать **Execute**
5. **Результат:** Ошибка 400 - "Нельзя играть с собой"

### Пример 3: Попробовать несуществующую задачу

1. Открыть **POST /matches/{match_id}/tasks** → "Добавить задачи в матч"
2. Нажать **"Try it out"**
3. Введите:
   ```json
   {
     "task_ids": [99999]
   }
   ```
4. Нажать **Execute**
5. **Результат:** Ошибка 404 - "Некоторые задачи не найдены"

---

## 📊 Все Endpoints

| Метод | Путь | Описание |
|-------|------|---------|
| POST | `/matches` | Создать новый матч |
| GET | `/matches` | Список активных матчей |
| GET | `/matches/{match_id}` | Информация о матче |
| POST | `/matches/{match_id}/tasks` | Добавить задачи в матч |
| POST | `/matches/{match_id}/answers` | Отправить/обновить ответ |
| POST | `/matches/{match_id}/finish` | Завершить матч и рассчитать рейтинг |

---

## 🔍 Что проверяется

### ✓ Функциональность

- [x] Создание матча с двумя игроками
- [x] Добавление задач в матч
- [x] Отправка ответов (INSERT новых)
- [x] Обновление ответов (UPDATE существующих) — UPSERT паттерн
- [x] Получение полной информации о матче с relationships
- [x] Завершение матча с расчётом рейтинга
- [x] Поиск активных матчей

### ✓ Constraints

- [x] CHECK: player1_id ≠ player2_id
- [x] UNIQUE: (match_id, user_id, task_id) для UPSERT
- [x] FK RESTRICT: задачи не удаляются при удалении матча
- [x] FK CASCADE: ответы удаляются при удалении матча

### ✓ Relationships

- [x] Match → player1 (User, lazy="joined")
- [x] Match → player2 (User, lazy="joined")
- [x] Match → tasks (MatchTask, lazy="selectin")
- [x] Match → answers (MatchAnswer, lazy="selectin")
- [x] MatchTask → task (Task, lazy="joined")
- [x] MatchAnswer → user (User, lazy="joined")
- [x] MatchAnswer → task (Task, lazy="joined")

---

## 🆘 Troubleshooting

### Ошибка: "Connection refused to localhost:5432"

**Решение:** Убедитесь что PostgreSQL запущена:
```bash
docker-compose ps | grep postgres
docker-compose up -d postgres
```

### Ошибка: "Матч не найден" (404)

**Решение:** Используйте правильный `match_id`. Проверьте response из шага 1.

### Ошибка: "Задача не найдена в этом матче" (404)

**Решение:**
1. Убедитесь что добавили задачу в матч (шаг 2)
2. Используйте правильный `task_id`

### Response slow?

**Решение:** Это нормально для первого запроса. Последующие будут быстрее.

---

## 📚 Дополнительные ресурсы

- **FastAPI документация:** https://fastapi.tiangolo.com
- **SQLAlchemy async:** https://docs.sqlalchemy.org/en/20/orm/extensions/asyncio.html
- **Swagger UI документация:** https://swagger.io/tools/swagger-ui/

---

## ✨ Что дальше?

После успешного тестирования через Swagger UI:

1. Добавить **real-time обновления** через WebSocket
2. Реализовать **matchmaking** — автоматический поиск противника
3. Интегрировать **Elo rating** — более сложный расчёт рейтинга
4. Добавить **notifications** — уведомления об окончании матча

---

**Дата:** 2026-02-05
**Версия:** 1.0
**Статус:** Production-ready ✓
