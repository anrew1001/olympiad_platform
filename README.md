# Olympeit - Платформа олимпиадного программирования

> Real-time платформа для соревновательного программирования с PvP матчами, системой рейтинга ELO и асинхронной архитектурой.

## 📋 Описание

Olympeit — это веб-платформа для практики и соревнований по олимпиадному программированию для учеников старших классов.

### Основные возможности

- **🎯 PvP матчи в реальном времени** - соревнуйтесь с другими участниками в матчах 1 на 1
- **📊 Система рейтинга ELO** - динамический рейтинг для отслеживания прогресса
- **📚 Каталог задач** - 60+ задач по математике, информатике и физике (10 класс)
- **⚡ WebSocket взаимодействие** - мгновенные обновления статуса матча без перезагрузки
- **🔄 Reconnection handling** - автоматическое восстановление соединения при обрыве связи
- **🏆 Таблица лидеров** - глобальный рейтинг лучших игроков
- **📈 Статистика игрока** - детальная аналитика по темам и сложности задач

## 🛠 Технологический стек

### Backend
- **Python 3.12** - современный Python с async/await
- **FastAPI** - высокопроизводительный web framework
- **PostgreSQL 14+** - основная база данных
- **SQLAlchemy 2.0** (async) - ORM с полной поддержкой async
- **WebSockets** - real-time двусторонняя коммуникация
- **JWT** - безопасная аутентификация
- **Pydantic** - валидация данных и сериализация

### Frontend
- **Next.js 15** (App Router) - React framework с SSR/SSG
- **TypeScript** - строгая типизация
- **Tailwind CSS** - utility-first CSS framework
- **WebSocket Client** - real-time коммуникация с сервером
- **Framer Motion** - плавные анимации

## 📦 Установка и запуск

### Требования
- **Python 3.12+**
- **Node.js 18+**
- **PostgreSQL 14+**
- **Docker** (опционально, для быстрого запуска PostgreSQL)

### 1. Клонирование репозитория

```bash
git clone https://github.com/your-username/olympiad-platform.git
cd olympiad-platform
```

### 2. Backend Setup

```bash
# Перейти в директорию backend
cd backend

# Создать виртуальное окружение
python3.12 -m venv venv_backend
source venv_backend/bin/activate  # Linux/Mac
# или
venv_backend\Scripts\activate  # Windows

# Установить зависимости
pip install -r requirements.txt

# Создать .env файл (скопировать из примера или создать вручную)
cat > .env << EOL
DATABASE_URL=postgresql+asyncpg://olympiad:olympiad@localhost:5432/olympiad
SECRET_KEY=your-secret-key-here-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30
EOL

# Запустить PostgreSQL (через Docker или локально)
docker-compose up -d postgres

# Инициализировать базу данных (создать таблицы)
python -m scripts.init_db

# Загрузить задачи из датасета
python -m scripts.load_grade10_mix

# Создать админ аккаунт (опционально)
python -m scripts.create_admin
# Username: admin, Password: admin123

# Запустить backend сервер
uvicorn app.main:app --reload
```

**Backend будет доступен:**
- API: http://localhost:8000
- Swagger документация: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

### 3. Frontend Setup

```bash
# Перейти в директорию frontend (из корня проекта)
cd frontend

# Установить зависимости
npm install

# Создать .env.local файл
cat > .env.local << EOL
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000
EOL

# Запустить dev сервер
npm run dev
```

**Frontend будет доступен:**
- Приложение: http://localhost:3000

## 📁 Структура проекта

```
olympiad_platform/
├── backend/                    # Backend приложение (FastAPI)
│   ├── app/
│   │   ├── main.py            # Точка входа FastAPI
│   │   ├── database.py        # SQLAlchemy setup, async engine
│   │   ├── models/            # ORM модели (User, Match, Task, etc.)
│   │   ├── routers/           # API endpoints (auth, pvp, tasks, users)
│   │   ├── services/          # Бизнес-логика (matching, elo, match_logic)
│   │   ├── schemas/           # Pydantic модели для валидации
│   │   ├── websocket/         # WebSocket handlers и ConnectionManager
│   │   ├── dependencies/      # FastAPI dependencies (auth, etc.)
│   │   └── utils/             # Утилиты (auth, validation)
│   ├── scripts/               # Инициализация и утилиты
│   │   ├── init_db.py        # Создание таблиц БД
│   │   ├── load_grade10_mix.py  # Загрузка задач
│   │   ├── create_admin.py   # Создание админа
│   │   └── add_test_users.py # Тестовые пользователи
│   ├── tests/                 # Unit и E2E тесты
│   │   ├── conftest.py       # Pytest fixtures
│   │   ├── services/         # Тесты сервисов
│   │   ├── websocket/        # WebSocket тесты
│   │   └── integration/      # Интеграционные тесты
│   └── requirements.txt       # Python зависимости
├── frontend/                   # Frontend приложение (Next.js)
│   ├── app/                   # Next.js App Router
│   │   ├── page.tsx          # Главная страница
│   │   ├── profile/          # Профиль пользователя
│   │   ├── pvp/              # PvP матчи
│   │   ├── tasks/            # Каталог задач
│   │   └── leaderboard/      # Таблица лидеров
│   ├── components/            # React компоненты
│   │   ├── ui/               # UI примитивы (Logo, LoadingScreen)
│   │   ├── layout/           # Навигация, layout
│   │   ├── tasks/            # Компоненты задач
│   │   ├── pvp/              # PvP компоненты (MatchHeader, TaskViewer)
│   │   ├── profile/          # Профиль (статистика, история)
│   │   └── leaderboard/      # Компонент таблицы лидеров
│   ├── lib/
│   │   ├── api/              # API клиенты (axios wrappers)
│   │   ├── hooks/            # Custom React hooks (useMatchWebSocket, etc.)
│   │   └── types/            # TypeScript типы
│   ├── context/               # React Context (AuthContext)
│   └── package.json           # Node.js зависимости
├── archive/                    # Архивные документы разработки
│   ├── documentation/         # Implementation guides, phase summaries
│   ├── testing/              # Тестовые скрипты и гайды
│   └── development_scripts/   # Временные dev скрипты
├── data/                       # Датасеты задач (grade10_mix.json)
├── docker-compose.yml          # PostgreSQL setup
├── SETUP.md                    # Детальная инструкция для разработчиков
└── README.md                   # Этот файл
```

## 🔌 API Эндпоинты

### Authentication (`/api/auth`)
- `POST /api/auth/register` - регистрация нового пользователя
- `POST /api/auth/login` - вход (получение JWT токена)
- `GET /api/auth/me` - получить текущего пользователя

### Tasks (`/api/tasks`)
- `GET /api/tasks` - список задач с фильтрами и пагинацией
  - Query params: `subject`, `difficulty`, `page`, `limit`
- `GET /api/tasks/{id}` - детали конкретной задачи

### PvP Matchmaking (`/api/pvp`)
- `POST /api/pvp/find` - найти или создать матч (автоматический matchmaking ±200 рейтинга)
- `POST /api/pvp/cancel/{match_id}` - отменить ожидающий матч
- `GET /api/pvp/match/{match_id}` - получить детали матча

### WebSocket (`/api/ws`)
- `WS /api/ws/pvp/{match_id}?token={jwt}` - real-time матч соединение
  - **События от сервера:** `player_joined`, `match_start`, `answer_result`, `match_end`, `opponent_disconnected`, `opponent_reconnected`, `disconnect_warning`, `reconnection_success`
  - **События от клиента:** `submit_answer`, `pong`

### Users (`/api/users`)
- `GET /api/users/leaderboard` - топ игроков по рейтингу (пагинация)
- `GET /api/users/{id}/stats` - статистика пользователя (матчи, задачи)

### Admin (`/api/admin`) - требует роль `admin`
- `GET /api/admin/tasks` - управление задачами
- `POST /api/admin/tasks` - создать задачу
- `PUT /api/admin/tasks/{id}` - обновить задачу
- `DELETE /api/admin/tasks/{id}` - удалить задачу

**Полная документация:** http://localhost:8000/docs (Swagger UI)

## 🧪 Тестирование

### Backend тесты

```bash
cd backend
source venv_backend/bin/activate

# Запустить все тесты
pytest tests/ -v

# Запустить конкретный модуль
pytest tests/services/test_elo.py -v

# Запустить с coverage
pytest tests/ --cov=app --cov-report=html
```

### Frontend проверка

```bash
cd frontend

# ESLint проверка
npm run lint

# TypeScript type checking
npm run type-check

# Production build (проверка что собирается без ошибок)
npm run build
```

### Manual E2E тестирование

1. **Регистрация и вход**
   - Зарегистрировать 2 пользователя
   - Войти под обоими аккаунтами (разные браузеры/инкогнито)

2. **Matchmaking и PvP**
   - Оба пользователя нажимают "Найти матч"
   - Проверить что матч создался и WebSocket подключился
   - Решить несколько задач
   - Проверить что счет обновляется в реальном времени

3. **Reconnection тест**
   - Во время матча отключить интернет на 10 секунд
   - Включить обратно
   - Проверить что состояние восстановилось корректно

4. **Leaderboard и статистика**
   - Проверить таблицу лидеров
   - Проверить профиль пользователя и статистику

## 🚀 Production Deployment

### Pre-deployment Checklist

- [ ] **Обновить CORS origins** в `backend/app/main.py` на production домен
- [ ] **Настроить `.env`** с production credentials (SECRET_KEY, DATABASE_URL)
- [ ] **PostgreSQL оптимизация** - пул соединений, индексы, vacuuming
- [ ] **HTTPS для WebSocket** - настроить WSS (secure WebSocket)
- [ ] **Rate limiting** - проверить настройки rate limiting в WebSocket
- [ ] **Логирование** - настроить Sentry, CloudWatch или аналоги
- [ ] **Мониторинг** - настроить health checks, metrics
- [ ] **Backup стратегия** - автоматические бэкапы PostgreSQL
- [ ] **Environment secrets** - использовать secrets manager (не хардкодить)

### Docker Deployment (опционально)

```bash
# Запустить только PostgreSQL через Docker
docker-compose up -d postgres

# TODO: Добавить Dockerfile для backend/frontend
# Полная контейнеризация запланирована в будущих релизах
```

### Environment Variables

**Backend (.env):**
```env
DATABASE_URL=postgresql+asyncpg://user:pass@host:5432/dbname
SECRET_KEY=your-very-long-secret-key-min-32-chars
ACCESS_TOKEN_EXPIRE_MINUTES=30
DISCONNECT_TIMEOUT_SECONDS=30
```

**Frontend (.env.local):**
```env
NEXT_PUBLIC_API_URL=https://api.your-domain.com
NEXT_PUBLIC_WS_URL=wss://api.your-domain.com
```

## 🏗 Архитектура

### WebSocket Reconnection System
- **Timeout:** 30 секунд (настраиваемый через `DISCONNECT_TIMEOUT_SECONDS`)
- **Progressive warnings:** Уведомления за 15s, 10s, 5s до форфейта
- **Flapping detection:** >3 отключения в 60s → штраф 50% (timeout сокращается до 15s)
- **Full state recovery:** При переподключении восстанавливается полное состояние матча

### ELO Rating System
- **K-factor:** 32 (фиксированный, Phase 1)
- **Формула:** `new_rating = old_rating + K * (actual_score - expected_score)`
- **Expected score:** `1 / (1 + 10^((opponent_rating - your_rating) / 400))`
- **Minimum rating:** 100 (защита от отрицательных значений)
- **Planned Phase 2:** Адаптивный K-factor в зависимости от опыта игрока

### Database Patterns
- **UPSERT для ответов** - атомарность под высокой нагрузкой (SELECT → UPDATE/INSERT)
- **SELECT FOR UPDATE** - критические секции (обновление рейтинга, финализация матча)
- **Per-message sessions** - WebSocket использует новую сессию для каждого сообщения (предотвращает deadlocks)
- **Master-only writes** - все записи идут в master БД (предотвращает конфликты репликации)
- **Lazy loading оптимизация** - `joinedload` для relationships, `noload` при FOR UPDATE

### Match Flow
1. **Matchmaking:** Пользователь нажимает "Найти матч" → сервис ищет waiting матч в диапазоне ±200 рейтинга
2. **Match Creation:** Если матч найден → присоединение, если нет → создание нового waiting матча
3. **WebSocket Connection:** Оба игрока подключаются к `/api/ws/pvp/{match_id}`
4. **Match Start:** Когда оба подключены → match status `ACTIVE`, генерируются 5 случайных задач
5. **Gameplay:** Игроки решают задачи, отправляют ответы → сервер проверяет и обновляет счет
6. **Match End:** Когда оба решили все 5 задач → подсчет победителя, обновление рейтингов
7. **Disconnect Handling:** Если игрок отключается >30s → автофорфейт

## 👨‍💻 Разработка

### Git Workflow

```bash
# Создать feature ветку
git checkout develop
git pull origin develop
git checkout -b feature/your-feature-name

# Работа над фичей
# ... ваши изменения ...

# Коммит
git add .
git commit -m "feat: add new feature description"

# Push и создание PR
git push origin feature/your-feature-name
# Создать Pull Request на GitHub в ветку develop
```

### Code Style

- **Backend:** Black (форматирование), Ruff (linting), mypy (type checking)
- **Frontend:** ESLint + Prettier (автоформатирование)
- **Комментарии:** Русский язык (для понимания логики)
- **Переменные и функции:** Английский язык (для совместимости)
- **Commit messages:** Conventional Commits (feat:, fix:, refactor:, docs:)

### Development Tips

- **Backend hot reload:** `uvicorn app.main:app --reload` автоматически перезапускает при изменениях
- **Frontend hot reload:** `npm run dev` автоматически обновляет страницу
- **Database migrations:** Используй Alembic для миграций (TODO: настроить)
- **WebSocket debugging:** Используй browser DevTools → Network → WS для отладки WebSocket

## 📚 Документация

- **API Reference:** http://localhost:8000/docs (Swagger)
- **Developer Guide:** [SETUP.md](./SETUP.md) - детальная инструкция для разработчиков
- **Architecture Docs:** [archive/documentation/](./archive/documentation/) - документы разработки

## 🐛 Известные проблемы и TODOs

- [ ] Alembic migrations не настроены (используем прямое создание таблиц)
- [ ] Dockerfile для backend/frontend (запланировано)
- [ ] CI/CD pipeline (GitHub Actions) не настроен
- [ ] Unit тесты для frontend компонентов (запланировано)
- [ ] Интернационализация (i18n) для мультиязычности
- [ ] Task submission с кодом (сейчас только числовые ответы)

## 🤝 Поддержка и вклад

### Reporting Issues
Если вы нашли баг или хотите предложить улучшение:
- GitHub Issues: https://github.com/your-username/olympiad-platform/issues

### Contributing
Pull requests приветствуются! Для больших изменений сначала откройте issue для обсуждения.

## 📄 Лицензия

MIT License - см. [LICENSE](./LICENSE)

## 👥 Авторы

- **Andrew Uglov** ([@andrewUG](https://github.com/andrewUG)) - Initial development
- **Claude Sonnet 4.5** - AI pair programming assistant

---

**Версия:** 1.0.0 (MVP)
**Последнее обновление:** Февраль 2026
**Статус:** Production Ready ✅
